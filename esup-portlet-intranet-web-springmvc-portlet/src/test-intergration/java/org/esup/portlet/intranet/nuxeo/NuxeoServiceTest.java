package org.esup.portlet.intranet.nuxeo;

import org.esup.portlet.intranet.BaseTestCase;
import org.junit.Test;

public class NuxeoServiceTest extends BaseTestCase{
	
	@Test
	public void testAll() throws Exception{
		
//		String intranetPath="/default-domain/workspaces/devq/";
//		int index1 = intranetPath.lastIndexOf('/');
//		int index2 = intranetPath.length();
//		if(index1 < 0 || index2 < 1)
//			throw new Exception("illegal intranet path : " + intranetPath);
//		if(index1 > 0 && index1 == index2-1){
//			intranetPath = intranetPath.substring(0, index2-1);
//			index1 = intranetPath.lastIndexOf('/');
//		}
//		String[] path = {intranetPath.substring(0,index1),intranetPath.substring(index1+1)};
//		printDocs("testGetList", nuxeoService.getList(prefs));
//		printDocs("testGetList", nuxeoService.getNews(prefs));
//		printDocs("testSearch", nuxeoService.search(prefs, key));
	}
	

}
